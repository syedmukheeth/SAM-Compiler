{
  "name": "@sam/shared",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "build": "node -e \"console.log('shared: no build step (plain JS)')\"",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
